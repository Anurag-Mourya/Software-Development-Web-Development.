> How is CSS different from CSS 3?
> What is the syntax for CSS?
> What are the different ways to apply CSS to a webpage?
> What are the different ways to apply CSS to a webpage?
> What is the difference between margin and padding?
> What are the different CSS border properties?
> What is the difference between CSS border and outline?
> What is the difference between display: none and visibility: hidden?
> What is the difference between flexbox and grid?
> What is the difference between class and id selector?
> Logical and Physical properties.
> What are CSS HSL Colors?.
> What are CSS backgrounds, list the properties?
> What is CSS Box Model?
> What does the CSS box-sizing property do and its property values content-box | border-box?
> How can we format text in CSS?
> What are the different CSS link states?
> What are the various positioning properties in CSS?
> What is CSS overflow?
> What does the CSS float property do?
> What does the CSS clear property do?
> Display with all- block, inline, inline-block.
> How can we vertically center a text and image in CSS?
> What are pseudo-classes and pseudo-element in CSS?
> What is meant by universal selector?
> What is RWD?
> What is specificity in CSS?
> What is z-index?

. SCSS
> What is SCSS and how is it different from CSS?
> What are SCSS variables?
> What is nesting in SCSS?
> What are mixins in SCSS?
> What is the @extend directive in SCSS?
> What is the difference between @extend and @include (mixins)?
> What are SCSS control directives?
> How do you compile SCSS to CSS?
> Advantages of SCSS